/**
 * Issue certificate — /app/issue
 * Placeholder: IssueForm ships in Phase 2b.
 */
export default function IssuePage() {
  return (
    <section className="space-y-6">
      <header>
        <h1 className="font-sans text-[clamp(1.5rem,3vw,2.25rem)] font-semibold text-ink tracking-tight">
          Issue a certificate
        </h1>
        <p className="mt-1 font-sans text-sm text-ink-muted">
          Mint a soulbound credential to a recipient wallet — coming in Phase 2b.
        </p>
      </header>
      <div className="rounded-xl border border-dashed border-border bg-surface px-6 py-10 text-center">
        <p className="font-sans text-sm text-ink-muted">Issue form placeholder.</p>
      </div>
    </section>
  )
}
